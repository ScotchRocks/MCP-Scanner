{
  "name": "mcp-security-scanner",
  "version": "1.0.0",
  "description": "Security scanner for MCP (Model Context Protocol) endpoints. Fetch a Trust Score for any MCP server.",
  "type": "module",
  "main": "src/index.js",
  "bin": {
    "mcp-scan": "./bin/mcp-scan.js"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "scan": "node bin/mcp-scan.js",
    "test": "node --test src/**/*.test.js",
    "prepublishOnly": "npm test"
  },
  "keywords": ["mcp", "security", "scanner", "ai", "model-context-protocol", "audit"],
  "license": "MIT",
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.0.0",
    "ora": "^8.0.1",
    "table": "^6.8.2"
  }
}